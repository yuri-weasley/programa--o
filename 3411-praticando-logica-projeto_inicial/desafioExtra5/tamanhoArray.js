let frutas = ['maca', 'banana', 'uva', 'morango', 'ranbutã', 'melancia', 'manga', 'laranja'];

function qualOTamanho(arr) {
    let tamanho = arr.length;
    console.log(tamanho);
}

qualOTamanho(frutas);